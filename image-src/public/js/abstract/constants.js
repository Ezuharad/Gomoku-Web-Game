export { NUM_ADJ_TO_WIN, NUM_SIDE_EDGES, PIECE_COLORS };

const NUM_ADJ_TO_WIN = 5;  // Number of adjacent pieces needed to win
const NUM_SIDE_EDGES = 15;  // Number of edges on the side of the board
const PIECE_COLORS = ["none", "black", "white"];  // Enumeration of colors
